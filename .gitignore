# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Herramientas
.pytest_cache/
.coverage
htmlcov/

# Entorno local (secretos van por variables de entorno, nunca al repo)
.env

# Logs de servicios locales (scripts/dev-run-services.ps1)
logs/
# Protege la sesión del CLI de Auth0 (.config/auth0/config.json). Regla de
# equipo: ningún archivo del SPA se llama config.json (quedaría ignorado).
config.json

# Node / front-end (apps/web-spa)
node_modules/
dist/
coverage/
*.local